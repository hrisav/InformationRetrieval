from setuptools import find_packages, setup

setup(
    name = 'Document Retrieval Project',
    version= '0.0.0',
    author= 'Hrisav',
    author_email= 'knightmusicriders@gmail.com',
    packages= find_packages(),            # help to install "src" (folder where __init__.py is present) as local package
    install_requires = []
)